
---

## 📁 `visualization/`

Модуль `visualization` отвечает за **отладочную и итоговую визуализацию** этапов анализа преформы. Все функции сохраняют изображения в папку `debug_output/` **только при включённых флагах отладки** из `config.py`.
Используется единый механизм сохранения через `utils.save_debug_image` или прямой вызов `cv2.imwrite` с учётом `config.DEBUG_OUTPUT_FOLDER`.

⚠️ **Важно:** Визуализация учитывает **ориентацию преформы** (`neck_left` / `neck_right`) — размеры отображаются с нужной стороны объекта.

---

### 📄 `visualization/contour_alignment_viz.py`

**Назначение:** Визуализация процесса **выравнивания контура** по главной оси с помощью PCA.

#### 🔧 Функции

*   **`visualize_contour_alignment(..., step_flag: bool = False)`**
    *   **Описание:** Создаёт отладочное изображение на **оригинальном (не повёрнутом) изображении**, показывающее:
        - исходный плотный контур преформы (зелёный),
        - габаритные точки (`leftmost`, `rightmost`, `topmost`, `bottommost` — красные),
        - центроид (красная точка),
        - **ось до поворота** — пунктирная линия, проходящая через центроид под углом PCA,
        - **ось после выравнивания** — сплошная линия, соответствующая горизонтальной оси выровненного контура, преобразованная обратно в исходную систему координат.
    *   **Условие сохранения:** Только если `step_flag=True` (обычно передаётся как `config.STEP_2_ALIGN_CONTOUR`).
    *   **Имя файла:** `<image_id>_09_aligned_contour_debug.jpg`
    *   **Особенности:**
        - Длина осевых линий масштабируется на основе **номинальной длины** из `dimensions_data` + запас 20 мм.
        - Все геометрические элементы корректно преобразуются между выровненной и исходной системами координат с помощью `rot_mat` и `cv2.invertAffineTransform`.

---

### 📄 `visualization/annotated_result_viz.py`

**Назначение:** Генерация **технического чертежа** в стиле CAD с нанесёнными размерами, адаптированного под ориентацию преформы.

#### 🔧 Функции

*   **`visualize_dimensioned_drawing(..., step_flag: bool = False)`**
    *   **Описание:** Создаёт полноценный чертёж:
        1. Исходное изображение **поворачивается** по матрице выравнивания (`rot_mat`).
        2. Преформа **сдвигается в центр холста** для симметричного отображения.
        3. На изображение наносятся:
            - контур преформы (зелёный),
            - центроид (красная точка),
            - **пунктирная осевая линия** (жёлтая),
            - габаритные точки (красные),
            - **размерные линии** с выносными линиями, стрелками и аннотациями для:
                - **длины (L)** — горизонтальный размер,
                - **ширины (Z)** — вертикальный размер,
                - **диаметра дна (d1)** — вертикальный размер в зоне дна.
        4. Текст отображается как `"L = 142.35 mm / 1139 px"` с фоном для читаемости.
    *   **Условие сохранения:** Только если `step_flag=True` (обычно `config.STEP_3_MEASURE_DIMENSIONS`).
    *   **Имя файла:** `<image_id>_10_size_visualization.jpg`
    *   **Особенности:**
        - **Адаптивное позиционирование размеров**:
            - Для **Z**: размер отображается **слева**, если горлышко слева (`neck_left`), и **справа** — если горлышко справа (`neck_right`).
            - Для **d1**: размер отображается **слева**, если дно слева (т.е. `neck_right`), и **справа** — если дно справа (`neck_left`).
        - Поддержка **вертикального текста** для вертикальных размеров (Z, d1).
        - Все стили (цвета, толщины, размеры стрелок, отступы) задаются в конфигурации `DEFAULT_CONFIG`.
        - Используется **универсальная функция** `draw_dimension_universal`, поддерживающая параметризацию направления, стрелок и выносных линий.

#### 🔧 Вспомогательные функции

*   **`draw_rotated_text(...)`**
    Рисует текст с прямоугольным фоном и поддержкой вертикальной ориентации.

*   **`draw_dashed_line(...)`**
    Рисует пунктирную линию заданной длины штриха.

*   **`draw_dimension_universal(...)`**
    Универсальный рендерер размерной линии с поддержкой:
    - горизонтальных и вертикальных размеров,
    - внутренних (`inward`) и внешних (`outward`) стрелок,
    - автоматического или ручного выбора стороны отображения,
    - адаптации под `neck_orientation`.

---

### 📄 `visualization/straightness_viz.py` *(новый файл)*

**Назначение:** Визуализация **прямолинейности оси преформы** на основе аппроксимации центральной линии.

#### 🔧 Функции

*   **`visualize_straightness(...)`**
    *   **Описание:** Отображает:
        - повёрнутую преформу на сером фоне,
        - **аппроксимирующую прямую** (жёлтая сплошная линия), построенную по методу наименьших квадратов,
        - **точки центров сечений** (зелёные точки) — середины между верхней и нижней границами контура,
        - **точку максимального отклонения** (красная точка),
        - **стрелку отклонения** (пурпурная) от аппроксимирующей прямой к точке максимума,
        - **границу горлышка** (жёлтая вертикальная линия + подпись `"Neck start"`),
        - **два числовых значения**:
            - `S_max = X.XXX mm` — максимальное отклонение,
            - `S_rms  = Y.YYY mm` — среднеквадратичное отклонение.
    *   **Условие сохранения:** Только если `config.STEP_10_STRAIGHTNESS = True`.
    *   **Имя файла:** `<image_id>_10_straightness.jpg`
    *   **Особенности:**
        - **Горлышко (последние 20% длины) исключено** из расчёта и визуализации осевой линии.
        - Все координаты корректно преобразуются с учётом матрицы поворота `rot_mat`.
        - Папка `debug_output/` создаётся автоматически при сохранении.

---

### 📌 Общие принципы визуализации

| Аспект | Реализация |
|-------|------------|
| **Сохранение файлов** | Прямой вызов `cv2.imwrite` с путём `config.DEBUG_OUTPUT_FOLDER / "<name>_<step>.jpg"` |
| **Управление включением** | Через булевы флаги из `config.py` (`STEP_2_*`, `STEP_3_*`, `STEP_10_STRAIGHTNESS`) |
| **Зависимости** | Только `cv2`, `numpy`, `config`, `loguru` — нет прямых вызовов `os` |
| **Безопасность** | Проверка пустых контуров, корректная обработка границ изображения |
| **Адаптивность** | Визуализация учитывает ориентацию преформы (`neck_left` / `neck_right`) и исключает горлышко при измерении прямолинейности |
| **Расширяемость** | Новые шаги легко добавляются как отдельные функции с собственными флагами |

---

### 🖼️ Примеры отладочных изображений

| Флаг | Файл | Содержимое |
|------|------|-----------|
| `STEP_2_ALIGN_CONTOUR = True` | `*_09_aligned_contour_debug.jpg` | Оригинал + оси PCA + габаритные точки |
| `STEP_3_MEASURE_DIMENSIONS = True` | `*_10_size_visualization.jpg` | Центрированный чертёж с размерами L, Z, d1 |
| `STEP_10_STRAIGHTNESS = True` | `*_10_straightness.jpg` | Аппроксимирующая прямая, центры сечений, S_max, S_rms, граница горлышка |

---
