---

## 📁 `preprocessing/`

Модуль `preprocessing` отвечает за **предварительную обработку** входного изображения и **подготовку контуров** для последующих измерений. Он включает:
- надёжное обнаружение основного контура преформы с многоуровневой фильтрацией,
- генерацию бинарной маски,
- подготовку плотного выровненного контура для детальных измерений (например, диаметра дна).

⚠️ **Важно:** В текущей версии **не используется инверсия маски** как резервный механизм. Отбраковка происходит сразу, если ни один контур не прошёл фильтрацию.

---

### 📄 `preprocessing/contour_detector.py`

**Назначение:** Обнаружение и первичная обработка контура преформы с жёсткой фильтрацией по форме, размеру и расположению.

#### 🔧 Функции

*   **`detect_contour(img: np.ndarray, image_id: Optional[str] = None) -> Tuple[Optional[np.ndarray], Optional[np.ndarray], Optional[np.ndarray]]`**
    *   **Описание:** Основная функция модуля. Выполняет полный цикл предобработки:
        1. Преобразование в градации серого,
        2. Гауссово размытие,
        3. Бинаризация (метод выбирается через `config.BINARY_METHOD`: `CANNY`, `OTSU`, `BACKGROUND_SUB_CANNY`, `DISTANCE_TRANSFORM`),
        4. Морфологическая очистка,
        5. Поиск внешних контуров,
        6. Фильтрация по трём критериям:
            - минимальная площадь (`MIN_CONTOUR_AREA`),
            - вытянутость (`aspect_ratio ≥ 2.0`),
            - отсутствие касания границ изображения (с отступом 2 пикселя).
        7. Выбор самого крупного валидного контура.
    *   **Аргументы:**
        *   `img` (np.ndarray): Входное BGR изображение.
        *   `image_id` (Optional[str]): Идентификатор изображения для логирования и отладки.
    *   **Возвращает:**
        *   `approx` (np.ndarray или None): Аппроксимированный контур `(Nx1x2)` — используется для выравнивания.
        *   `mask_filled` (np.ndarray или None): Бинарная маска с **тонкой линией по контуру** (толщина = 1 пиксель), а не заполненная область.
        *   `selected_contour` (np.ndarray или None): Исходный (плотный, не аппроксимированный) контур `(Nx1x2)` — используется для измерения дна.
    *   **Особенности:**
        - При отсутствии валидных контуров возвращает `(None, None, None)`.
        - Поддерживает отладочную визуализацию при включённом `config.STEP_1_DETECT_CONTOUR`.

*   **`is_elongated_contour(contour: np.ndarray, min_aspect_ratio: float = 2.0) -> bool`**
    *   **Описание:** Проверяет, является ли объект вытянутым, используя ориентированный ограничивающий прямоугольник (`cv2.minAreaRect`). Отсеивает компактные объекты (например, капли, шум).
    *   **Возвращает:** `True`, если соотношение сторон ≥ `min_aspect_ratio`.

*   **`is_contour_touching_border(contour: np.ndarray, img_shape: Tuple[int, int, ...], border_margin: int = 2) -> bool`**
    *   **Описание:** Проверяет, касается ли контур границ изображения с учётом небольшого отступа (`border_margin` пикселей). Такие объекты считаются обрезанными и отбраковываются.
    *   **Возвращает:** `True`, если контур касается хотя бы одной границы.

*   **`apply_morphology(binary: np.ndarray, kernel_size: int) -> np.ndarray`**
    *   **Описание:** Применяет последовательность морфологических операций:
        - **Закрытие** (`MORPH_CLOSE`) — заполняет мелкие разрывы,
        - **Открытие** (`MORPH_OPEN`) — удаляет мелкие шумы,
        - **Дилатация** — утолщает оставшиеся структуры.
    *   **Используется** в методах `OTSU` и `BACKGROUND_SUB_CANNY`.

*   **Внутренние функции (`_binarize_otsu`, `_binarize_background_subtraction_canny`, `_binarize_distance_transform`)**
    *   **Описание:** Реализуют специфические стратегии бинаризации в зависимости от `config.BINARY_METHOD`. Не экспортируются.

#### 📌 Особенности фильтрации

- **Тройная проверка:** контур должен одновременно быть достаточно большим, вытянутым и полностью внутри изображения.
- **Чёткая отбраковка:** если ни один контур не прошёл фильтрацию — функция возвращает `None`, и анализ останавливается на этапе `analyzer.py`.
- **Отладка:** при включённом `STEP_1_DETECT_CONTOUR` сохраняются все промежуточные изображения и визуализация всех контуров (валидные — зелёные, отбракованные — красные).

---

### 📄 `preprocessing/dense_contour.py`

**Назначение:** Подготовка **плотного выровненного контура** для точных измерений (например, диаметра дна), с учётом геометрических преобразований.

#### 🔧 Функции

*   **`get_aligned_dense_contour(selected_contour: np.ndarray, rot_mat: np.ndarray, flip_mat: Optional[np.ndarray] = None) -> Optional[np.ndarray]`**
    *   **Описание:** Применяет к исходному плотному контуру:
        1. Матрицу поворота выравнивания (`rot_mat`), полученную из `measurement.geometry.align_contour`,
        2. (Опционально) матрицу поворота на 180° (`flip_mat`), если бы применялась коррекция ориентации.
    *   **Аргументы:**
        *   `selected_contour` — исходный контур из `detect_contour`,
        *   `rot_mat` — матрица аффинного поворота `(2×3)`,
        *   `flip_mat` — матрица переворота (в текущем pipeline **всегда `None`**, так как ориентация не корректируется физически).
    *   **Возвращает:** Плотный контур в выровненной системе координат `(Nx1x2)`, готовый для `measure_bottom_diameter`.
    *   **Примечание:** В текущей версии `flip_mat` не используется, но функция оставлена для гибкости и будущих расширений.

---

### 📌 Ключевые изменения по сравнению с предыдущей версией

- **Удалён механизм инверсии маски** — отбраковка теперь строгая.
- **Маска — тонкая линия**, а не заполненная область (важно для PCA в `geometry.py`).
- **Добавлена проверка касания границ** с отступом — повышает надёжность отбраковки обрезанных преформ.
- **Поддержка нескольких методов бинаризации**, включая `DISTANCE_TRANSFORM` и `BACKGROUND_SUB_CANNY`.
- **Функция `get_aligned_dense_contour` больше не применяет переворот**, так как ориентация определяется метаданными, а не геометрией.

---
